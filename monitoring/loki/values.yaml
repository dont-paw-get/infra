# grafana/loki Helm chart
# 단일 바이너리(SingleBinary) 모드로 시작한다. 트래픽/보존 요구가 커지면 SimpleScalable 모드로 전환 검토.

deploymentMode: SingleBinary

loki:
  auth_enabled: false

  commonConfig:
    replication_factor: 1

  # TODO(open issue, ADR-0001): 오브젝트 스토리지(S3 등) 연동 여부 확정 전까지 로컬 파일시스템 사용.
  storage:
    type: filesystem

  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: loki_index_
          period: 24h

  # TODO(open issue, ADR-0001): 로그 보존 기간 확정 전까지의 임시값.
  limits_config:
    retention_period: 336h # 14d

singleBinary:
  replicas: 1
  persistence:
    enabled: true
    size: 20Gi

gateway:
  enabled: true

test:
  enabled: false
