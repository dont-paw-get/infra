# Grafana Alerting webhook 통합(monitoring/alerting/contact-points/discord.yaml)이 이 Service를 호출한다.
apiVersion: v1
kind: Service
metadata:
  name: rca-agent
  namespace: monitoring
spec:
  selector:
    app: rca-agent
  ports:
    - port: 8080
      targetPort: 8080
