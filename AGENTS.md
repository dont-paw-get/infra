## 하네스: 크로스 툴 작업 연속성

**목표:** Claude Code, Codex, Kiro 중 어떤 도구로 세션을 시작하든, 이전 작업 맥락(계획·진행상황·인수인계)을 이어서 파악하고 작업할 수 있게 한다.

**세션 시작 시 반드시 먼저 읽을 것 (이 순서로):**

1. `.harness/HANDOFF.md` — 직전 세션이 어디서 멈췄는지
2. `.harness/STATE.md` — 지금까지 무엇이 완료되었는지
3. `.harness/ARCHITECTURE.md` — 관측 스택 구조 요약 (저장소 재탐색 최소화)
4. `.harness/PLAN.md` — 제안·확정·진행 중인 계획
5. 필요 시 `.harness/DECISIONS.md`(과거 결정 이유 — `docs/adr/`의 ADR과 중복 기록하지 않고 참조만), `.harness/BACKLOG.md`(미해결 항목)

이 저장소에는 도메인 aggregate가 없으므로 `DOMAIN.md`는 사용하지 않는다.

**문서별 책임 (중복 기록 금지 — 아래 표에 없는 문서에는 해당 내용을 쓰지 않는다):**

| 문서              | 담는 내용                                                                              | 담지 않는 내용                                                            |
| ----------------- | -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| `HANDOFF.md`      | 세션마다 무엇을 했는지 (append-only 서술형 로그)                                       | 단계별 완료 요약(STATE 몫), 결정 이유(DECISIONS/ADR 몫)                   |
| `STATE.md`        | 지금까지 끝난 것의 단계 단위 요약 스냅샷                                               | 세션별 서술(HANDOFF 몫). 이슈 하나하나를 로그처럼 쌓지 않는다             |
| `ARCHITECTURE.md` | 지금의 관측 스택 구성(Helm 릴리스, 네임스페이스, 데이터소스 uid 등)·컨벤션 (현재 상태) | 왜 그렇게 정했는지(ADR 몫), 진행 상황(STATE 몫)                           |
| `DECISIONS.md`    | `docs/adr/`에 없는 소규모/운영 결정의 역사(append-only)                                | 아키텍처 수준 결정(ADR 몫 — `docs/adr/`에 작성), 구현 진행 상황(STATE 몫) |
| `PLAN.md`         | 아직 안 끝난 계획과 체크리스트만                                                       | 완료된 항목(체크만 남기지 말고 STATE로 옮긴 뒤 제거)                      |
| `BACKLOG.md`      | 지금 하지 않지만 나중에 할 것(오픈 이슈·기술부채·아이디어)                             | 진행 중인 계획(PLAN 몫)                                                   |

아키텍처 수준 결정(스택 선정, 호스팅 방식, CR 소유권 등)은 `.harness/DECISIONS.md`가 아니라 `docs/adr/`가 소유한다. `.harness`는 ADR을 복제하지 않고 참조한다.

**작업 워크플로우 (필수):**

- 새로운 변경 요청을 받으면, 바로 구현하지 말고 `.harness/PLAN.md`에 계획 초안을 작성한다.
- 사용자에게 계획을 제시하고 피드백을 받아 반영하는 과정을 반복한다.
- 사용자가 명시적으로 컨펌하면 계획을 확정 상태로 바꾸고 구현을 시작한다.
- 다음은 계획 절차 없이 바로 수행한다: 설명·조사·리뷰처럼 파일을 변경하지 않는 요청, 오탈자나 명백한 단순 수정(수정 전 무엇을 바꾸는지 한 줄로 알린다).
- `PLAN.md` 단계별 체크리스트 항목은 하나씩 구현이 끝날 때마다 즉시 `.harness/STATE.md`에 반영하고, 그 항목을 `PLAN.md`에서 제거한다.
- 구현 완료 후 `.harness/STATE.md`를 갱신한다.
- 세션을 종료하거나 작업을 중단할 때 `.harness/HANDOFF.md`에 다음 세션을 위한 인수인계를 남긴다.
- 아키텍처 수준 결정을 내리면 `docs/adr/`에 새 ADR을 추가한다 (기존 ADR 수정이 아니라 새 번호로 추가하고, 기존 ADR을 대체하면 그 ADR의 상태를 "대체됨"으로 갱신).

**트리거:** 이 저장소에서의 모든 작업 요청에 위 워크플로우를 적용하라. 단순 질문(구성 설명 등)은 하네스 절차 없이 바로 응답 가능.

## 하네스: 변경 산출물 동기화

**목표:** 한 변경이 여러 산출물에 걸쳐 있을 때, 설정을 고치면서 관련 문서를 빠뜨리지 않는다.

**단일 소유권:**

| 정보                                                    | 소유 산출물         |
| ------------------------------------------------------- | ------------------- |
| 아키텍처 수준 결정과 이유(스택 선정, 호스팅 방식 등)    | `docs/adr/*.md`     |
| 크로스 툴 인수인계·진행 상황·현재 구성 현황·계획·백로그 | `.harness/*.md`     |
| 저장소 진입점, 빠른 시작, 구조 개요                     | 루트 `README.md`    |
| 시크릿 관리 정책                                        | `secrets/README.md` |

**변경 시 함께 갱신할 것:**

- **Helm values 변경** (`monitoring/*/values.yaml`): 기본값/보존기간/threshold를 바꿨다면 `.harness/ARCHITECTURE.md`의 관련 서술 갱신. 아키텍처 수준 결정(스택 교체, 호스팅 방식 변경 등)이면 `docs/adr/`에 새 ADR 추가.
- **알림 규칙/연락처 변경** (`monitoring/alerting/`): threshold나 라우팅을 바꿨다면 관련 ADR의 "미결정" 목록에서 해당 항목을 제거하거나 갱신.
- **`ServiceMonitor` 소유권 등 서비스 저장소와의 경계 관련 결정**: `docs/adr/0001-observability-stack.md`를 갱신하고, 서비스 저장소 쪽에 전달이 필요하면 사용자에게 명시적으로 알린다 (이 저장소에서 서비스 저장소 파일을 직접 수정하지 않는다).
- **완료된 계획 항목**: `.harness/PLAN.md`에서 제거하고 `.harness/STATE.md`에 단계 한 줄로 반영.
- **README.md에 링크·안내가 걸린 파일을 이동·삭제·이름변경**: `README.md`의 해당 링크를 같은 작업에서 수정하거나 제거 (깨진 링크를 남기지 않는다).

**절차:**

1. 변경 전에 위 표에서 어떤 산출물이 영향을 받는지 식별한다.
2. 설정 변경과 관련 산출물 갱신을 같은 작업(같은 커밋 또는 같은 PR)에서 처리한다.
3. 오래된 경로·이름·예제·상태가 남아있지 않은지 확인한다.
4. 갱신한 산출물과 갱신하지 못한 산출물을 사용자에게 한국어로 보고한다.

## 하네스: 관측 스택 정책

**목표:** 이 저장소가 소유하는 범위와 핵심 결정을 빠르게 상기한다. 전체 근거는 `docs/adr/0001-observability-stack.md` 참고.

- 스택: Prometheus + Grafana + Loki (`kube-prometheus-stack` Helm 차트), 로그 수집은 Alloy DaemonSet.
- 호스팅: 클러스터 내 자체 호스팅 (매니지드 서비스 미사용).
- 알림: Alertmanager 대신 Grafana Alerting을 사용하고 Discord contact point로 발송한다. `alertmanager.enabled: false`를 유지한다.
- `ServiceMonitor`/`PodMonitor` CR은 각 서비스 저장소(`backend-book` 등)가 소유한다 — 이 저장소에는 두지 않는다.
- 이 저장소는 수집·저장·시각화·알림 파이프라인만 소유한다. 계측 지점(메트릭 엔드포인트, 구조화 로깅)은 서비스 저장소 책임이다.
- 시크릿(Discord 웹훅 URL, Grafana admin 비밀번호 등)은 어떤 파일에도 하드코딩하지 않는다. `secrets/README.md`의 정책을 따른다.

## 하네스: 변경 검증 정책

**목표:** Claude Code, Codex, Kiro 중 어떤 도구로 작업하든 배포 전 동일한 최소 검증을 거친다.

- Helm values를 수정하면 적용 전 렌더링을 확인한다: `helm template <release> <chart> -f <values.yaml>`로 문법 오류와 예상치 못한 리소스 변경이 없는지 점검한다.
- Helm 차트/저장소가 로컬에 추가되어 있다면 `helm lint`도 함께 실행한다.
- Grafana Alerting provisioning YAML(`monitoring/alerting/`)을 수정하면 YAML 문법(들여쓰기, `datasourceUid` 참조)을 재확인한다 — 배포 전 실제 클러스터에서 sidecar가 정상적으로 반영하는지 확인은 사용자가 명시적으로 요청했을 때 수행한다.
- 위 검증은 파일을 실제로 클러스터에 적용하지 않는 범위에서 수행한다. `scripts/install.sh` 실행이나 `kubectl apply`는 사용자가 명시적으로 요청했을 때만 수행한다.

## 하네스: 브랜치·커밋·병합 전략

**목표:** Claude Code, Codex, Kiro 중 어떤 도구로 작업을 시작하든 팀의 실제 Jira 흐름과 커밋 컨벤션을 동일하게 따른다.

- 원격 저장소는 `origin` (`https://github.com/dont-paw-get/infra.git`)이며 `main`과 `develop`을 갖는다.
- 작업 브랜치는 `develop`에서 분기하고 이름은 `{티켓번호}-{설명}` 형식을 사용한다 (예: `CLIAR-82-관측-스택`). `feature/...` 형식은 사용하지 않는다.
- 현재 브랜치가 진행 중인 작업과 다른 티켓이면, 구현 시작 전에 해당 티켓 번호로 새 브랜치를 `develop`에서 생성하고 전환한다. 이미 해당 티켓 브랜치에 있다면 새 브랜치를 만들지 않는다.
- 문서·설정 전용 작업도 코드 변경과 동일하게 티켓 브랜치에서 진행한다. main에 직접 커밋하는 예외는 두지 않는다.
- 커밋 메시지 컨벤션: `<타입>[적용 범위(선택)]: <제목>` 구조. 타입은 영어(`feat`, `fix`, `docs`, `style`, `refactor`, `chore`), 제목/본문은 한국어.
  - 제목은 명사형 어미로 끝내고 50자 이내, 마침표 없음.
  - scope는 작업한 영역(예: `alerting`, `loki`)을 명시하면 이력 추적에 유리하다.
  - 제목 또는 본문에 관련 티켓 번호를 `[CLIAR-82]`처럼 표기한다.
- 커밋은 사용자가 명시적으로 요청했을 때만 생성한다. 관련 파일을 골라 stage하고, `git add .`/`git add -A`는 피한다.
- 한 티켓 브랜치에서 작업이 끝나면 PR을 `develop`으로 생성할 수 있지만, PR 생성과 push는 사용자가 명시적으로 요청했을 때만 수행한다.
- 브랜치 병합과 삭제는 자동으로 수행하지 않는다. PR 병합은 사용자가 직접 하거나, 사용자가 명시적으로 요청했을 때만 수행한다. `develop → main` 릴리스 병합도 동일하다.
- 강제 push, `reset --hard`, `clean -fd`, `branch -D` 등 destructive 작업은 사용자의 명시적 허락 없이 수행하지 않는다.
